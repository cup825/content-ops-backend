### 分页API测试指南

## 前置条件
- 系统已启动：http://localhost:8080
- 数据库中已有用户和权限数据
- Admin用户ID为1（X-User-Id: 1）

---

## 测试1：用户列表 - 分页获取（基础分页）
### 请求
GET http://localhost:8080/api/admin/users/page?page=1&pageSize=5

### 预期响应 (200 OK)
```json
{
  "content": [
    {
      "id": 1,
      "username": "admin",
      "roleName": "ADMIN",
      "createdAt": "2026-04-22T10:00:00"
    },
    {
      "id": 2,
      "username": "operator1",
      "roleName": "OPERATOR",
      "createdAt": "2026-04-22T10:01:00"
    }
  ],
  "total": 10,        // 数据库中一共有10个用户
  "page": 1,          // 当前是第1页
  "pageSize": 5,      // 每页5条
  "totalPages": 2     // 共2页
}
```

---

## 测试2：用户列表 - 分页 + 排序（倒序）
### 请求
GET http://localhost:8080/api/admin/users/page?page=1&pageSize=5&sortBy=id&sortOrder=desc

### 说明
- `sortBy=id` 按ID排序
- `sortOrder=desc` 倒序（新建的用户在前）

### 预期响应
用户数据按ID倒序排列，最新的用户在最前面

---

## 测试3：用户列表 - 第2页
### 请求
GET http://localhost:8080/api/admin/users/page?page=2&pageSize=5

### 预期响应
```json
{
  "content": [
    {
      "id": 6,
      "username": "operator5",
      "roleName": "OPERATOR",
      "createdAt": "2026-04-22T10:05:00"
    },
    // ... 更多用户
  ],
  "total": 10,
  "page": 2,          // ← 现在是第2页
  "pageSize": 5,
  "totalPages": 2
}
```

---

## 测试4：权限列表 - 分页获取
### 请求
GET http://localhost:8080/api/admin/permissions/page?page=1&pageSize=10&sortBy=id&sortOrder=asc

### 预期响应
```json
{
  "content": [
    {
      "id": 1,
      "permissionName": "CREATE_CONTENT"
    },
    {
      "id": 2,
      "permissionName": "EDIT_CONTENT"
    }
  ],
  "total": 6,
  "page": 1,
  "pageSize": 10,
  "totalPages": 1
}
```

---

## 测试5：获取单个角色
### 请求
GET http://localhost:8080/api/admin/roles/1

### 预期响应 (200 OK)
```json
{
  "id": 1,
  "roleName": "ADMIN"
}
```

---

## 测试6：更新角色（预期失败 - 因为角色不可修改）
### 请求
PUT http://localhost:8080/api/admin/roles/1
Headers:
  X-User-Id: 1
Body:
```json
{
  "roleName": "SUPER_ADMIN"
}
```

### 预期响应 (400 Bad Request)
```json
{
  "code": 400,
  "message": "角色为系统预定义，不支持修改",
  "data": null
}
```

---

## 测试7：删除角色（预期失败 - 因为角色不可删除）
### 请求
DELETE http://localhost:8080/api/admin/roles/1
Headers:
  X-User-Id: 1

### 预期响应 (400 Bad Request)
```json
{
  "code": 400,
  "message": "角色为系统预定义，不支持删除",
  "data": null
}
```

---

## 测试8：无权限操作（非ADMIN用户尝试分页查询用户）
### 请求
GET http://localhost:8080/api/admin/users/page?page=1&pageSize=10
Headers:
  X-User-Id: 2  (假设2是OPERATOR用户)

### 注意
分页查询本身不需要权限校验（查询操作不受限），但其他修改操作需要权限

---

## 快速验证清单

- [ ] 测试1：分页获取用户，检查 `total`, `page`, `totalPages` 字段
- [ ] 测试2：带排序参数，检查用户顺序（倒序）
- [ ] 测试3：请求第2页，检查 `page=2`
- [ ] 测试4：权限分页，检查权限数据
- [ ] 测试5：单个角色查询成功
- [ ] 测试6：修改角色返回400错误
- [ ] 测试7：删除角色返回400错误
- [ ] 测试8：查询接口返回正确数据

---

## 常见问题排查

### Q1: 返回 "404: Cannot resolve method 'getUsersByPage'"
**原因**：Service或Controller未正确修改
**解决**：重新编译项目 (Ctrl+Shift+F9 或 Build → Rebuild Project)

### Q2: 分页响应格式不对
**原因**：可能是PaginationResponse DTO没有正确导入
**解决**：检查Controller是否导入了 `import com.bytedance.content.admin.dto.PaginationResponse;`

### Q3: 排序不生效
**原因**：sortBy 字段名不对（应该是数据库字段名）
**解决**：确认字段名：`id`, `username`, `createdAt` 等

### Q4: page=1 但返回的是第0页的数据
**原因**：PaginationRequest.getPageNumber() 的逻辑有问题
**解决**：确认 `page - 1` 的计算正确


